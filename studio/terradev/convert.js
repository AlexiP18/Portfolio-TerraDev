const fs = require('fs'); let svg = fs.readFileSync('logo_terrabyte.txt', 'utf8'); svg = svg.replace(/class=/g, 'className='); svg = svg.replace(/fill-rule/g, 'fillRule'); svg = svg.replace(/clip-rule/g, 'clipRule'); svg = svg.replace(/xml:space/g, 'xmlSpace'); svg = svg.replace(/shape-rendering/g, 'shapeRendering'); svg = svg.replace(/text-rendering/g, 'textRendering'); svg = svg.replace(/image-rendering/g, 'imageRendering'); svg = svg.replace(/style=\"shape-rendering:geometricPrecision;text-rendering:geometricPrecision;image-rendering:optimizeQuality;fillRule:evenodd;clipRule:evenodd\"/g, 'style={{shapeRendering:\"geometricPrecision\", textRendering:\"geometricPrecision\", imageRendering:\"optimizeQuality\", fillRule:\"evenodd\", clipRule:\"evenodd\"}}'); svg = svg.replace(/style=\"fill:([#a-fA-F0-9]+)\"/g, 'style={{fill: \"\"}}'); svg = svg.replace(/<style>(.*?)<\/style>/s, '<style dangerouslySetInnerHTML={{ __html: \\\$1\\\ }} />'); svg = svg.replace(/width=\"120mm\" height=\"80mm\"/, 'className=\"h-8 w-auto\"'); fs.writeFileSync('logo_react.txt', svg); console.log('done');
